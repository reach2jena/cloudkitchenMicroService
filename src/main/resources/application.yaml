spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/jenascloudkitchen?allowPublicKeyRetrieval=true&useSSL=false  
    username: root
    password: P@55word
    hikari:
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  application:
    name: JENASCLOUDKITCHEN
server:
  port: 8045
  
